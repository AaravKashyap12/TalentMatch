# Web framework
fastapi
uvicorn[standard]
python-multipart
pydantic[email]
python-dotenv

# NLP — lightweight pipeline (no torch/transformers)
spacy
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
thinc
numpy

# ML / similarity — TF-IDF only (no sentence-transformers, no torch, no onnxruntime)
# Removing sentence-transformers + onnxruntime saves ~500 MB RAM on Render free tier.
# TF-IDF cosine similarity is used instead — ~2 MB footprint, 0-second cold-start.
scikit-learn

# PDF extraction — pypdf is primary (pure-Python, low RAM), pdfplumber as fallback
pypdf
pdfplumber

# Database — async drivers for SQLite (dev) and Postgres (prod)
sqlalchemy[asyncio]
aiosqlite
asyncpg

# Auth / security
python-dateutil

# API / infra  (APScheduler removed — keep-alive scheduler was pinning models in RAM)
slowapi
structlog

# Testing
pytest
pytest-asyncio
httpx
