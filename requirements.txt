# Web framework
fastapi
uvicorn[standard]
python-multipart
pydantic[email]
python-dotenv

# NLP
spacy
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
thinc
numpy

# ML / similarity
nltk
scikit-learn
sentence-transformers
# Lightweight ONNX runtime instead of full PyTorch (~10 MB vs 800 MB+)
# sentence-transformers uses ONNX automatically when onnxruntime is present
onnxruntime
optimum[onnxruntime]

# PDF extraction
pypdf
pdfplumber

# Database — async drivers for SQLite (dev) and Postgres (prod)
sqlalchemy[asyncio]
aiosqlite
asyncpg

# Auth / security
python-dateutil

# API / infra
slowapi
structlog

# Testing (not needed in production — install with: pip install -r requirements.txt -r requirements-dev.txt)
# Kept here for simplicity; move to requirements-dev.txt when desired
pytest
pytest-asyncio
httpx
